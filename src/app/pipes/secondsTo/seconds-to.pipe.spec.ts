import { SecondsToPipe } from './seconds-to.pipe';

describe('SecondsToPipe', () => {
  it('create an instance', () => {
    const pipe = new SecondsToPipe();
    expect(pipe).toBeTruthy();
  });
});
